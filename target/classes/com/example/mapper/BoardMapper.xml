<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.BoardMapper">
    <insert id="insert">
        <![CDATA[
            INSERT INTO board(num, mid, subject, content, readcount, reg_date, ipaddr, re_ref,re_lev, re_seq)
            VALUES(#{num}, #{mid}, #{subject}, #{content}, #{readcount}, #{reg_date}, #{ipaddr}, #{re_ref}, #{re_lev}, #{re_seq})
        ]]>
    </insert>

    <select id="nextNum" resultType="int">
        <![CDATA[
            SELECT IFNULL(MAX(num), 0) + 1 AS bnum
            FROM board;
        ]]>
    </select>

    <delete id="deleteAll">
        <![CDATA[
            DELETE FROM board
        ]]>
    </delete>
</mapper>